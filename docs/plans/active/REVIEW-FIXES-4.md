# Backend Review Fixes 4 — Wave-4 Audit Plan

> **STATUS: ACTIVE — planned for execution on main (`6c80391`).** Wave-4 findings from the 2026-08-17 full audit (docs/plans + `.superpowers/sdd` reconciliation, PRD-alignment and money-correctness bugs in the booking/payment/room/support domains, and third-party readiness — Xendit is **not** production-compatible, Resend/Google Meet/R2 need fail-loud guards). Findings verified in code at HEAD `6c80391` and against current vendor API docs (Xendit OpenAPI 2024-11-11).

> **For agentic workers:** REQUIRED SUB-SKILL: superpowers:subagent-driven-development (recommended) or superpowers:executing-plans. Steps use checkbox (`- [ ]`) syntax.

**Goal:** Close every verified wave-4 concern — docs reconciled with reality (move REVIEW-FIXES-3 to completed, fix CONTEXT/README/plan statuses, resolve the `.superpowers/sdd` centralization question), 3 CRITICAL + 6 HIGH + 9 MEDIUM backend correctness bugs fixed with tests, the Xendit provider rewritten against the current API (with a provider refund port), fail-loud guards added for Resend/Google Meet/R2, and RUNBOOK/env examples corrected for the real-credentials swap.

**Architecture:** independent PR groups, backend-first, all targeting `main`. Follows the existing 4-layer pattern, `DbOrTx`, `DomainError` + `withDomainMap`, bounded zod, consumer-driven ports, and real-DB integration tests.

**Tech Stack:** Bun 1.3.14, Elysia, oRPC, Drizzle + postgres.js, BullMQ, better-auth 1.6.11, Cloudflare R2, bun:test, oxlint/oxfmt, GitHub Actions.

## Global Constraints

- Import from `@cogito-app/...` package paths; modules use `../../lib`, `../../shared`, `../../procedures`.
- 4-layer pattern; `DbOrTx` (`packages/api/src/lib/tx.ts`); `DomainError` + `withDomainMap`; bounded zod.
- **RPC protocol facts:** HTTP paths are oRPC procedure keys with slashes — `/rpc/payment/createPurchase`, NOT dotted names. Bodies use the `{"json": <input>}` envelope; responses come back as `{"json": <data>, "meta": [...]}`.
- **Docs follow code (AGENTS.md rule 11):** every PR updates `docs/CONTEXT.md`, `docs/RUNBOOK.md` where touched, the affected plan files, and this plan's statuses in the same PR.
- Conventional commits; one commit per task or small coherent group.
- Verify per task: `bun run check-types`, `bun run lint`, `bunx oxfmt --check`, targeted `env-file` tests.
- Full suite (plain run, GOOGLE_MEET unset): `GOOGLE_MEET_ENABLED=false GOOGLE_MEET_REFRESH_TOKEN= GOOGLE_MEET_CLIENT_ID= GOOGLE_MEET_CLIENT_SECRET= GOOGLE_CLIENT_EMAIL= GOOGLE_PRIVATE_KEY= bun test --env-file apps/server/.env packages/api/src/tests/ apps/server/src/openapi.test.ts`, plus `bun test --env-file apps/server/.env apps/server/src/` in a **separate process** (webhook TTL test uses `mock.module`).
- Coverage gates: `packages/api` ≥ 90% lines, overall ≥ 80%. Baseline: API 1803 pass / 0 fail, server 44 pass / 0 fail; api 97.6%, overall 97.5%.
- Docker DB+Redis: `bun run db:start` (Postgres 6767, Redis 6379). Redis is mandatory.
- Working tree must be clean (`git status`) and on `main` before each PR branch.

---

## Concern Inventory (verified 2026-08-17)

### Docs / plans / `.superpowers/sdd`

| ID  | Finding                                                                                                                                                                                                                                                                                                                                                 | Location                                                              | PR  |
| --- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | --------------------------------------------------------------------- | --- |
| D1  | REVIEW-FIXES-3 plan still in `active/` with every checkbox [x] except G2 and a "planned for execution" header — all 6 PRs (#59–#65) merged. Violates its own completion instruction ("When all PRs land, move to completed/").                                                                                                                          | `docs/plans/active/REVIEW-FIXES-3.md`                                 | P1  |
| D2  | PRD-GAPS-PHASE3 header claims "all U-items closed (U1–U14 implemented)" but U9 (support SLA business-hours) is Partial in the same file and in code. Intro line still lists open items that are now implemented.                                                                                                                                        | `docs/plans/active/PRD-GAPS-PHASE3.md:5,12,28`                        | P1  |
| D3  | Phantom RPC names documented as implemented: `booking.rescheduleSelf` and `tutorActions.approveReschedule` don't exist anywhere in `apps/`/`packages/`. Actual surface is `booking.proposeReschedule` (studentProcedure) + `booking.acceptReschedule` (protected).                                                                                      | `REVIEW-FIXES-3.md:240`, `PRD-GAPS-PHASE3.md:21,64`                   | P1  |
| D4  | DEFERRED-OPS 1.4 claims 7 bare `.select()` remain in `booking.repo.ts`; actual count is **0** (all converted).                                                                                                                                                                                                                                          | `docs/plans/active/DEFERRED-OPS-TASKS.md:37`                          | P1  |
| D5  | CONTEXT.md Plans table is badly stale: REVIEW-FIXES-2 listed Active (merged #50–#57); BACKEND-CLEANUP listed under active path (moved to completed); PRD-GAPS-PHASE3 status row lists U-items as open; FRONTEND branch/status wrong; REVIEW-FIXES-3 missing from table; execution order still says "Review Fixes 2 → next to execute".                  | `docs/CONTEXT.md:337-370`                                             | P1  |
| D6  | CONTEXT API-module lists stale: AdminBooking omits `setMeetingLink` + `cancelSeriesSession`; TutorActions omits `markParticipantNoShow`; Booking omits `getRescheduleAvailability`; Achievement omits `listApproved` and still says "No public achievement endpoint exists yet" (false). "16 routers" is wrong — actual **17**.                         | `docs/CONTEXT.md:209,240-245,260-264,272,290`                         | P1  |
| D7  | C6 (password policy) status contradictorily stated 3 ways in CONTEXT.md: closed (line 317), Partial (line 469), open (line 526). Code: fully implemented (`packages/auth/src/index.ts:26-37` enforced at `apps/server/src/routes.ts:257`).                                                                                                              | `docs/CONTEXT.md:317,469,526`                                         | P1  |
| D8  | Migration numbers in plans are wrong vs the merged Drizzle journal: B6 unique index is **0019** (not "0022"); achievement PRD fields migration is **0023** (not "0020"); session `session_id` column is in **0020** (not "0021"). Also CONTEXT:427 cites `listBookingsByState` at `admin-booking.repo.ts:31-33`; actual is `:94`.                       | `PRD-GAPS-PHASE3.md:26,29`, `REVIEW-FIXES-3.md:161`, `CONTEXT.md:425` | P1  |
| D9  | `docs/plans/README.md` index stale: REVIEW-FIXES-3 row "Planned"; DEFERRED-OPS "1.4 partial: 7 bare selects"; PRD-GAPS row lists open U-items; FRONTEND row lists F8/F13/F14/F16 open (all closed by P6).                                                                                                                                               | `docs/plans/README.md:7-10`                                           | P1  |
| D10 | `.superpowers/sdd/.gitignore` is `*` — only 3 progress.md files are tracked; `BACKEND-HARDENING/` (69 files, ~1.9 MB of briefs/reports/review-diffs) and `BACKEND-HARDENING-PHASE2/` are **untracked local history** at risk of loss. Structural question (user): keep `.superpowers/sdd` or centralize in `docs/plans/`? See P1.7 for the disposition. | `.superpowers/sdd/`                                                   | P1  |

### Backend money-correctness / lifecycle (verified in code)

| ID  | Severity | Finding                                                                                                                                                                                                                                                                                                                                                                                                                 | Location                                                                                             | PR    |
| --- | -------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ---------------------------------------------------------------------------------------------------- | ----- |
| C1  | CRITICAL | `markParticipantNoShow` on a **group** (non-series) transitions the whole booking to NO_SHOW, zeroes `holdAmount`, and never releases/deducts the other confirmed participants' holds — NO_SHOW is terminal and the release/expire jobs skip it, so their marks are stranded forever. Violates PRD lateness rule (prd.tex:749-750: one student's no-show must not cancel the class; they forfeit only their own marks). | `booking.service.ts:1419-1514` (esp. 1486-1493)                                                      | P2.1  |
| C2  | CRITICAL | Student can bypass the H-2 late-cancel penalty via `proposeReschedule`: the only guard checks the **new** time is ≥2h out; there is no check the **current** session is still >H-2 away. A student 30 min before class proposes any slot ≥2h out; if accepted the hold survives free; if rejected the original slot is restored — also free. Never invoked by a cancel.                                                 | `booking.service.ts:1576-1604` (`proposeReschedule` student branch)                                  | P2.2  |
| C3  | CRITICAL | `completeSession` (solo/group, non-series) has **no "session started" time guard** — only a `SCHEDULED` state check. A tutor can deduct the student's held Marks immediately after accepting (or at any time before the session). `completeSeriesSession` DOES have the guard (`BookingSessionNotStartedError` at :1112-1114). Inconsistent and money-draining.                                                         | `booking.service.ts:1015-1027` vs `:1085-1114`                                                       | P2.3  |
| H1  | HIGH     | Accepted reschedule returns the booking to `SCHEDULED`/`AWAITING_ADMIN_ROOM_APPROVAL` but the deadline stays at the proposal's `now+24h` (set at propose); only the `AWAITING_TUTOR_REVIEW` target bumps it. `expireBookings` treats SCHEDULED with a passed deadline as NO_SHOW and **releases all holds** — a reschedule accepted for a session 2 weeks out is auto-cancelled/refunded ~24h later.                    | `booking.service.ts:1685-1689` (propose), `:1780-1786` (accept), `:3148-3152`, `:3263-3299` (expire) | P2.4  |
| H2  | HIGH     | Tutor-lateness auto-cancel is flawed both ways: (a) `markTutorAttendance` only checks `SCHEDULED`, so a tutor can mark "present" days early and skip the session — the `NOT EXISTS` lateness query never fires (student protection gone); (b) a tutor who teaches but never clicks attendance gets the session auto-cancelled and all holds released at start+15..20min (false-positive money leak).                    | `booking.repo.ts:651-679`, `booking.service.ts:1516-1552`                                            | P2.5  |
| H3  | HIGH     | `relocateRoom` never calls `bookingPort.transitionBookingToScheduled` (unlike `assignRoom`). A relocated booking still in `AWAITING_ADMIN_ROOM_APPROVAL` keeps its `min(now+12h, start)` deadline and gets cancelled + holds released by `expireBookings` even though a room was confirmed.                                                                                                                             | `room.service.ts:173-227` vs `:157-159`                                                              | P2.6  |
| H4  | HIGH     | REFUNDED webhook auto-reversal wedges when the student already spent the Marks: `wallet.compensate` → `atomicCompensateDeduct` requires `availableBalance >= amount`; failure throws `InsufficientBalanceError` inside the tx → whole tx rolls back (status stays PAID) → webhook 500s → provider retries forever. PRD TC-39 requires admin reconciliation in this case, not a rollback loop.                           | `payment.service.ts:297-321`, `wallet.repo.ts:194-209`, `wallet.service.ts:359-373`                  | P2.7  |
| H5  | HIGH     | Support SLA is a flat 12h (`SUPPORT_SLA_MS`) with no business-hours/WIB computation and **no WhatsApp escalation** to +62 881-0119-90195; the escalation job only flips an `escalated` flag + audit row. Also no auto-ack on ticket creation. PRD OQ-04 requires 30 min (Mon–Sat 09:00–21:00 WIB) / 4h otherwise + WhatsApp. Known (documented "partial"), still a PRD gap that blocks launch.                          | `constants.ts:11`, `support.service.ts:76,114-137`                                                   | P2.8  |
| H6  | HIGH     | `applyOverride` transitioning a booking to a terminal state never calls `meeting.cancelEvent` — a force-cancelled class keeps a live Google Meet link. Every other terminal path cancels the meeting. Overrides also don't cancel meetings on state change (only `setMeetingLink` uses the meeting port here).                                                                                                          | `admin-booking.service.ts:252-402`                                                                   | P2.9  |
| M1  | MED      | `applyOverride` with a `marksAction` but empty `affectedParticipants` silently does the state change and sets the terminal booking's holds stranded (no release, no deduct, and terminal states are skipped by the release job).                                                                                                                                                                                        | `admin-booking.service.ts:195-250`                                                                   | P2.9  |
| M2  | MED      | `expireBookings` SCHEDULED→NO_SHOW branch **releases** participant holds instead of forfeiting (PRD requires no-show → deduct). The "forgot to click anything" default gives money back instead of enforcing the forfeit. Also SCHEDULED deadline is `scheduledEnd + 24h`, so this can fire a day after a real class.                                                                                                   | `booking.service.ts:3263-3299`, `:3043-3047`                                                         | P2.10 |
| M3  | MED      | `cancel()` of a confirmed **group series** by the proposer bypasses the no-opt-out guard (`withdraw` has `BookingSeriesNoOptOutError`, `cancel` doesn't) — pulls the whole class and releases every participant's holds.                                                                                                                                                                                                | `booking.service.ts:731-855` vs `:2330-2332`                                                         | P2.11 |
| M4  | MED      | `releaseExpiredHolds` releases holds without a state transition (deadline passed + transition failed/raced). A later tutor accept/complete then deducts from a zero hold → `InsufficientBalanceError` → a delivered-but-unpaid session.                                                                                                                                                                                 | `booking.service.ts:3342-3392`                                                                       | P2.12 |
| M5  | MED      | Reconfirmation-decline and withdraw-pre-H2 paths don't refresh `deadlineAt` to `now+12h`, so remaining participants can see a sub-12h (or already-passed) reconfirmation window until the next expiry sweep re-adds it.                                                                                                                                                                                                 | `booking.service.ts:2264-2318`, `:2467-2478`                                                         | P2.13 |
| M6  | MED      | `cancelRoomBooking` (admin) only cancels the roomBooking row + notifies; the booking stays `AWAITING_ADMIN_ROOM_APPROVAL` until its deadline expires it with hold release + no explicit audit. PRD FR-22 expects cancellation with hold release + audit on "cancel if no room available".                                                                                                                               | `room.service.ts:229-253`                                                                            | P2.14 |
| M7  | MED      | Withdraw from a group in `AWAITING_ADMIN_ROOM_APPROVAL` regresses to reconfirmation but leaves the `requested` roomBooking row live; an admin `assignRoom` mid-reconfirmation inserts a confirmed room row for a booking that then goes back to tutor review.                                                                                                                                                           | `booking.service.ts:2442-2478`                                                                       | P2.13 |
| M8  | MED      | Group reprice during a pre-H2 `withdraw` throws `InsufficientMarksError` and rolls back the withdrawal (a participant is stuck in a group they can't leave). PRD TC-19 expects the group to fall through to expiry on unfunded reprice (the `expireBookings` B5 path already handles it).                                                                                                                               | `booking.service.ts:375-381` (`repriceGroupForHeadcount`)                                            | P2.15 |
| M9  | MED      | Knowledge Bank eligibility endpoint is `protectedProcedure` (any role), PRD FR-12 is student-facing; tutors/admins with ≥35 marks pass the gate. Also `notification.eventKey` unique index is global (not per-user) — fine today (all keys embed ids) but a latent 500.                                                                                                                                                 | `wallet.router.ts:35-42`, `notification.ts:53`                                                       | P2.16 |
| L1  | LOW      | `completeSeriesSession` deducts a full per-session hold after an admin `cancelSeriesSession(..., release)` — later deduct can exceed remaining hold → `InsufficientBalanceError`. Guard with `Math.min`.                                                                                                                                                                                                                | `admin-booking.service.ts:703-740`, `booking.service.ts:1135-1148`                                   | P2.16 |
| L2  | LOW      | Meeting event row written on the **global** `db`, not the booking tx (`finalizeMeetingSchedule` runs inside `tutorAccept`'s tx) — tx rollback after Google event creation leaves an orphan event + `meetingEvent` row; tutor re-accept duplicates the Google event.                                                                                                                                                     | `google-meeting.provider.ts:605-615`, `booking.service.ts:881-895`                                   | P2.17 |
| L3  | LOW      | "Meeting link ready" notification is sent even when the fallback provider (`manual`/`pending`, no URL) is used — misleading copy to students. Should emit a "link pending" message when the meeting row has no URL.                                                                                                                                                                                                     | `booking.service.ts:3049-3075`                                                                       | P2.17 |
| L4  | LOW      | Xendit webhook timestamp validation uses `x-timestamp`/`date` headers; Xendit documents only `x-callback-token`. If Xendit omits a `Date` header, every webhook 408s. Verify against a real sandbox event; make the check provider-conditional if needed.                                                                                                                                                               | `apps/server/src/webhooks/payments.ts:30-43`                                                         | P3.5  |
| L5  | LOW      | `infra/.env.prod.example` / RUNBOOK env table stale: missing R2_*, `GOOGLE_IMPERSONATED_USER`, `GOOGLE_MEET_*`, `WEBHOOK_ALLOWED_IPS`, `SEED_*`; RUNBOOK references non-existent `RESEND_FROM_EMAIL` (actual `EMAIL_FROM`). `monitoring.md` has a stale line about Redis health ownership.                                                                                                                              | `infra/*.example`, `docs/RUNBOOK.md`, `infra/monitoring.md`                                          | P1/P4 |

### Third-party readiness (verified in code + vendor docs 2026-08-17)

| ID  | Integration    | Verdict             | Key blocker(s)                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                      | PR   |
| --- | -------------- | ------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ---- |
| X1  | Xendit         | **NOT READY**       | Provider implements the **legacy** pre-2024 v3 request shape. Current API (`api-version: 2024-11-11`) requires top-level `type:"PAY"`, `country:"ID"`, `currency:"IDR"`, `request_amount`, `channel_code` (OVO/DANA/QRIS/BCA), `channel_properties:{success_return_url,failure_return_url}`, `customer` for e-wallets. Response is a **top-level** object (no `data` wrapper) and `actions[].value` (no `.url`). Statuses are `SUCCEEDED` (not PAID), `REQUIRES_ACTION`, `AUTHORIZED`, `CANCELED`. Webhook payload has `data.payment_id`/`payment_request_id`, **no `event_id`** (idempotency key collapses to `xendit:no-event-id` for every payment → cross-payment collision). No provider **refund** call exists in `PaymentProvider` — `adminRefund` only reverses wallet Marks, real money is never returned. | P3   |
| X2  | Resend         | **ALMOST READY**    | Provider + consumers are correct (POST `/emails`, Bearer, `Idempotency-Key`, 30s AbortController, circuit breaker, outbox dedup). Missing prod guard: `RESEND_API_KEY` is optional with no `NODE_ENV=production` requirement → silent stub, all critical emails suppressed with no alert. `EMAIL_FROM` domain (cogitoacademy.id) must be verified at Resend; not documented. G2 email verification still not implemented.                                                                                                                                                                                                                                                                                                                                                                                           | P4.1 |
| X3  | Google Meet    | **ALMOST READY**    | Both auth paths (OAuth refresh token + service account JWT) are correctly implemented (scopes, `conferenceDataVersion:1`, `hangoutsMeet` createRequest, token cache, circuit breaker, retry job, manual fallback). Missing: guard on `GOOGLE_IMPERSONATED_USER` for service-account mode (without domain-wide delegation events land on the SA's own calendar and never get a Meet URL); `.env.prod.example` documents exactly that broken config; no loud boot validation when `GOOGLE_MEET_ENABLED=true` + missing creds (falls back silently); no RUNBOOK section on obtaining the OAuth refresh token.                                                                                                                                                                                                          | P4.2 |
| X4  | Cloudflare R2  | **ALMOST READY**    | Presigned POST (SigV4, policy conditions incl. x-amz-* and content-length-range) is correct for R2 (region `auto`). Missing: `NODE_ENV=production` guard for R2 vars → prod without R2 writes to container-local `UPLOAD_DIR`, lost on every redeploy; R2 configured but `R2_PUBLIC_URL` unset → objects unreachable (`GET /uploads/*` disabled when R2 set, but URL resolution returns bare key); env examples contain no R2 vars.                                                                                                                                                                                                                                                                                                                                                                                 | P4.3 |
| X5  | WhatsApp / SLA | **NOT IMPLEMENTED** | No WhatsApp adapter (documented as out-of-scope-until-approved). SLA window computation (business hours WIB) not implemented (H5).                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                  | P2.8 |

---

## PR P1 — Docs & planning-layout reconciliation (docs-only, mostly)

**Goal:** every active plan + CONTEXT + README reflects verified code state; `.superpowers/sdd` disposition decided and recorded.

- [x] **1.1** Move `docs/plans/active/REVIEW-FIXES-3.md` → `docs/plans/completed/`; add a completed status header with merged PR list (#59–#65), note G2 still deferred → tracked in REVIEW-FIXES-4 P4.1/P4.4.
- [x] **1.2** `docs/plans/active/PRD-GAPS-PHASE3.md` (D2, D8): header → "all U-items closed except U9 partial"; intro paragraph updated; fix migration numbers (U10 → 0023, U7 → 0020, B6/U8 note → 0019); fix phantom `rescheduleSelf`/`approveReschedule` names (D3) → `proposeReschedule`/`acceptReschedule`; add U9 note → aligned to REVIEW-FIXES-4 P2.8.
- [x] **1.3** `docs/plans/active/DEFERRED-OPS-TASKS.md` (D4): 1.4 note → 0 bare `.select()` remain (P5.8 of wave-3 actually landed); remove the stale "tracked in REVIEW-FIXES-3 P5.8".
- [x] **1.4** `docs/CONTEXT.md` (D5–D8): Plans table rows (REVIEW-FIXES-2 → completed, BACKEND-CLEANUP → completed, PRD-GAPS-PHASE3 → all closed except U9 partial, FRONTEND branch `f/frontend-prd-gaps` + merged, add REVIEW-FIXES-3 completed + REVIEW-FIXES-4 active); execution order updated (wave-3 done, wave-4 next); API-module lists: 17 routers, add `setMeetingLink`/`cancelSeriesSession`/`markParticipantNoShow`/`getRescheduleAvailability`/`listApproved`; remove "No public achievement endpoint exists yet"; C6 row → Fully implemented (single consistent statement); `admin-booking.repo.ts:94`; add wave-4 findings table (C1–C3, H1–H6, M1–M9, L1–L5).
- [x] **1.5** `docs/plans/README.md` (D9): index rows corrected (REVIEW-FIXES-3 → completed w/ PRs; REVIEW-FIXES-4 added; DEFERRED-OPS 1.4 done; PRD-GAPS all-closed-except-U9; FRONTEND F-items closed).
- [x] **1.6** `docs/RUNBOOK.md`: fix `RESEND_FROM_EMAIL` → `EMAIL_FROM`; add "Real-provider swap" section (env table for Resend/Xendit/GOOGLE_MEET/R2 + what fails loud vs silent); Xendit sandbox verification checklist (L4); Google Meet refresh-token acquisition steps (X3); note `STUB_WEBHOOK_ALLOWED` required on staging for payment E2E.
- [x] **1.7** `.superpowers/sdd` disposition (D10): **RESOLVED 2026-08-17 (user decision: keep + commit histories).** `.superpowers/sdd` stays as the execution ledger (worktree paths, commit ranges, test counts, merge reconciliation); `docs/plans/` stays as the durable plans. `.superpowers/sdd/.gitignore` relaxed from `*` to `/*` + `!**/progress.md` + `!BACKEND-HARDENING/` + `!BACKEND-HARDENING-PHASE2/`, so the formerly untracked BACKEND-HARDENING (69 files) + BACKEND-HARDENING-PHASE2 (9 files) history is now committed (scanned for secrets first — only test fixtures). Two-file-per-plan rule enforced: plan in `docs/plans/`, ledger in `.superpowers/sdd/{PLAN}/progress.md`.
- [x] **1.8** Verify `bunx oxfmt --check` clean; commit `docs: reconcile plans, CONTEXT, and README with verified wave-4 audit; decide .superpowers/sdd disposition`.

---

## PR P2 — Backend money-correctness & lifecycle fixes (TDD; highest priority)

> All TDD: failing test first, then implementation. Money paths must use `wallet.hold/release/deduct/credit/compensate` inside the booking transaction with deterministic `eventKey`s (ledger idempotency via UNIQUE(wallet_id,event_key,source_reference)).

- [x] **2.1 (C1)** Group `markParticipantNoShow`: only forfeit the target participant's hold, mark their `attendanceState`, keep the booking state; recompute booking `holdAmount` = sum of remaining confirmed participants' held amounts (mirror the group branches in `completeSingleSession`/`completeSeriesSession`). Tests: group no-show does not transition booking, does not strand other holds; cancel/expire still release remaining holds later; solo still transitions to NO_SHOW. — `booking-no-show-group.test.ts` (done `2026-08-17`)
- [x] **2.2 (C2)** `proposeReschedule` student branch: also require the **current** `b.scheduledStartAt - now > LATE_CANCEL_THRESHOLD_MS` (mirror `cancel()`). Tests: session 30 min away → `BookingNotEditableError`; session 6h away → guarded only by new-slot H-2 check as today; group/series apply same rule at booking level. — `booking-reschedule-h2.test.ts` (done `2026-08-17`)
- [x] **2.3 (C3)** `completeSingleSession`: add `if (b.scheduledStartAt.getTime() > Date.now()) throw new BookingSessionNotStartedError(...)` (parity with series). Tests: completing before start rejected; at/after start accepted; attendee/lateness edge (start+15min) unaffected. — `booking-complete-guard.test.ts` (done `2026-08-17`)
- [x] **2.4 (H1)** `acceptReschedule`: when target state is `SCHEDULED`/`AWAITING_ADMIN_ROOM_APPROVAL`, set `deadlineAt = proposedEndAt + OFFLINE_SCHEDULED_GRACE_MS` for offline or `scheduledEnd`-based grace for online (mirror `transitionBookingToScheduled` / `finalizeMeetingSchedule`). Tests: reschedule accepted 2 weeks out does not expire 24h later; AWAITING_TUTOR_REVIEW target still gets 12h. — `booking-reschedule-deadline.test.ts` (done `2026-08-17`)
- [x] **2.5 (H2)** `markTutorAttendance`: restrict to `[scheduledStartAt − LATENESS_TOLERANCE_MS, scheduledStartAt + LATENESS_TOLERANCE_MS]` (15 min each side) so tutors can't pre-mark to dodge lateness (`BookingNotEditableError` outside the window). `checkTutorLateness` no longer auto-cancels or releases holds: it keeps each unmarked SCHEDULED session live with holds intact, merges `overrideMeta.category = "tutor_lateness_pending"` + `flaggedAt` (preserving other keys) via the versioned booking update so `adminBooking.listBookings({ category })` surfaces it in the admin queue, writes a `tutor_lateness_pending_review` audit record, and notifies the proposer + tutor (INFO). Returns `{ flagged, failed }`. Tests both directions. — `tutor-lateness.test.ts` (rewritten to assert flagging) + `tutor-lateness-window.test.ts` (done `2026-08-17`; fix round: admin-category filter moved to a dedicated `OVERRIDE_LIST_CATEGORIES` enum (`listOverridesInput` RPC now reachable with `tutor_lateness_pending`), and the lateness sweep excludes already-flagged bookings so repeat sweeps are idempotent — no duplicate audit/notification rows)
- [x] **2.6 (H3)** `relocateRoom`: call `bookingPort.transitionBookingToScheduled(tx, bookingId, actorId)` when the booking is still `AWAITING_ADMIN_ROOM_APPROVAL` (safe no-op via the existing guard at `booking.service.ts:2957`). Tests: relocate from awaiting → booking becomes SCHEDULED; from scheduled → no-op. — `room-relocate.test.ts` (done `2026-08-17`)
- [x] **2.7 (H4)** REFUNDED webhook: if `availableBalance < record.marks`, do not auto-reverse with a thrown error. Instead: mark the payment REFUNDED, record the mismatch (audit + `refundRecord` or a reconciliation flag), and surface a reconciliation item for admin per TC-39. Keep the existing R5 reversal for the clean case. Tests: spent-marks REFUNDED webhook → status REFUNDED + no wallet hurl + reconciliation row; clean case still reverses. — `refund-flow.test.ts` (H4 cases) + `payment.service.test.ts` (done `2026-08-17`)
- [x] **2.8 (H5)** Support SLA: business-hours-aware `slaDeadline` implemented — `isBusinessTimeWib` (Mon–Sat 09:00–21:00 WIB = UTC+7) + `computeSlaDeadline` (30 min business / 4 h otherwise, wall-clock per OQ-04); auto-ack notification on ticket creation (`support.{id}.acknowledged`); escalation job extended to emit a `support.{id}.escalated` notification row (metadata `whatsappTarget: "+6288101190195"`, `escalate: true`) as the hook a future WhatsApp adapter consumes (adapter NOT built). `SUPPORT_SLA_MS` replaced by `WIB_UTC_OFFSET_HOURS`/`SUPPORT_SLA_BUSINESS_MINUTES`/`SUPPORT_SLA_OFF_HOURS_HOURS`/`SUPPORT_BUSINESS_*` constants. Tests: `support-sla.test.ts` (SLA math for WIB windows), `support.service.test.ts` (auto-ack + escalation hook), `support-flow.test.ts` (integration, updated SLA assertion + escalation hook row). — (done `2026-08-17`)
- [x] **2.9 (H6 + M1)** `applyOverride`: after the override transaction commits, `meeting.cancelEvent(bookingId)` is called best-effort for terminal transitions (mirroring `cancel()` — a provider failure is logged, never breaks the override); `planOverride` requires a non-empty `affectedParticipants` whenever a `marksAction` is provided, otherwise `OverrideMarksParticipantsRequiredError` (400) — a money action is never a silent no-op. `AdminBookingMeetingPort` gained `cancelEvent`; `meeting` dep is optional with a guard for `setMeetingLink`. Tests: `admin-booking.service.test.ts` (H6 cancel/non-terminal/best-effort, M1 missing/empty participants) + `admin-booking.errors.test.ts` (error + 400 mapping). — (done `2026-08-17`)
- [x] **2.10 (M2)** `expireBookings` SCHEDULED→NO_SHOW: the no-show (post-start) branch now **deducts** each confirmed participant's held amount (`booking.{id}.no_show.{userId}`, `system` actor, participant hold zeroed) instead of releasing — enforcing the PRD no-show forfeit; release remains for genuinely pre-start expiry states (EXPIRED/CANCELLED). Notification copy: "held marks were forfeited". Tests: `booking-expiry-no-show.test.ts` (new integration: solo SCHEDULED → NO_SHOW + wallet deduct + ledger), `scheduler-expiry.test.ts` (state-mapping + differential cases updated for the forfeit), `booking.service.test.ts` (unit: deduct not release). — (done `2026-08-17`)
- [x] **2.11 (M3)** `cancel()`: group-series no-opt-out guard (`BookingSeriesNoOptOutError`) applied for `type === SERIES && targetGroupSize > 1` once past `AWAITING_PARTICIPANT_CONFIRMATION` (mirrors `withdraw`; admin override remains the escape hatch). Pre-confirmation cancellation still works — the terminal target falls back to EXPIRED where CANCELLED is unreachable (mirrors withdraw's `cancelTarget` logic). Tests: `booking.service.test.ts` (unit: confirmed/awaiting-review blocked, pre-confirmation allowed, solo-series unaffected) + `booking-group-series.test.ts` (integration: proposer `cancel` rejected with CONFLICT on the confirmed series). — (done `2026-08-17`)
- [x] **2.12 (M4)** `releaseExpiredHolds` → transition-or-skip: the terminal transition (shared `EXPIRY_TARGET` with `expireBookings`) is applied **first** in the same tx, then holds are released (or forfeited for NO_SHOW, mirroring M2); version-conflict/terminal/RESCHEDULE_PROPOSED bookings are skipped without touching the wallet — a later tutor accept/complete can never deduct from a zeroed hold. Tests: `booking.service.test.ts` (version-conflict skip, transition-first, terminal skip, RESCHEDULE_PROPOSED skip, SCHEDULED forfeit), `scheduler-holds.test.ts` + `booking-no-show-group.test.ts` updated for the new semantics. — (done `2026-08-17`)
- [ ] **2.13 (M5 + M7)** Reconfirmation-decline and withdraw-pre-H2 paths: `updateBookingDeadline(now + RESPONSE_WINDOW_MS)` when entering `AWAITING_RECONFIRMATION`; on withdraw from `AWAITING_ADMIN_ROOM_APPROVAL`, mark the `requested` roomBooking row cancelled. Tests.
- [ ] **2.14 (M6)** `cancelRoomBooking`: add a room-port call to cancel the booking (transition + release) in the same transaction with an audit reason. Tests.
- [ ] **2.15 (M8)** `withdraw` pre-H2 reprice: catch `InsufficientMarksError` from `repriceGroupForHeadcount` → fall through to the expiry branch (`releaseAll + EXPIRED`) matching the B5 handling. Tests.
- [ ] **2.16 (M9 + L1)** KB eligibility → `studentProcedure` (router change + test); `completeSeriesSession` deduct → `Math.min(session.holdAmount, remainingHeld)`. Tests.
- [ ] **2.17 (L2 + L3)** Move `meetingEvent` insert inside the booking tx (provider accepts `db`; check `DbOrTx` propagation), or document + guard the out-of-tx write (delete orphan row on rollback); fix the "link ready" copy to "link pending" when no URL. Tests.
- [ ] **P2 docs:** CONTEXT known-bugs table rows; this plan's checkboxes.

---

## PR P3 — Xendit provider rewrite against current API + refund port (TDD)

- [ ] **3.1** Test-first rewrite of `createIntent` (`xendit-payment.provider.ts:60-139`) to the 2024-11-11 schema: `POST /v3/payment_requests` with `api-version: 2024-11-11` header, body `{reference_id, type:"PAY", country:"ID", currency:"IDR", request_amount, channel_code, channel_properties:{success_return_url, failure_return_url}, customer:{reference_id, given_names, email, mobile_number}, customer_notification_preference (OVO), metadata:{paymentId}}`. Map channel codes: `ewallet_ovo → OVO`, `qris → QRIS`, `va_bca → BCA`. Update `PAYMENT_METHOD_CONFIG` and unit mocks.
- [ ] **3.2** Response parse: top-level object; `actions` entries now `{type:"REDIRECT_CUSTOMER", value, descriptor}` → pick `actions.find(a => a.type==="REDIRECT_CUSTOMER")?.value`, fall back to `PRESENT_TO_CUSTOMER` value for QRIS. Update response-shape mocks in `xendit-payment.provider.test.ts` (they currently encode the legacy `.url` shape and would keep passing while real integration is broken).
- [ ] **3.3** Status mapping: `SUCCEEDED→PAID`, `REQUIRES_ACTION→PENDING`, `AUTHORIZED→PENDING/PAID`, `CANCELED→FAILED`, keep `PENDING/FAILED/EXPIRED`; handle `payment_request.*` event types. Update `mapXenditStatus`.
- [ ] **3.4** Webhook parse (`xendit-payment.provider.ts:156-188`): derive `providerEventId` from `data.payment_id ?? data.payment_request_id ?? body.payment_id`, `providerReference` from `data.reference_id`; stop relying on `event_id`/`receipt_url` (`receipt_url` → from `payment_details.receipt_id` if needed). This fixes the `xendit:no-event-id` key collision (webhooks/payments.ts:91).
- [ ] **3.5** Webhook timestamp: verify against a real Xendit sandbox event whether a `Date`/`x-timestamp` header is sent; if not, make timestamp validation provider-conditional (skip for xendit or fall back to no-check). Document Xendit webhook source IP ranges for `WEBHOOK_ALLOWED_IPS`.
- [ ] **3.6** Add `refund(paymentRequestId, amountIdr, reason?)` to `PaymentProvider` + call `POST /v3/payment_requests/{id}/refunds` from `adminRefund` (`admin-booking.service.ts:483-576`) and store the provider refund id on `refundRecord`. Stub provider returns a mock refund id. (Additive; confirm the exact refund endpoint shape from the Refund tag in the Xendit OpenAPI during implementation.)
- [ ] **3.7** `XENDIT_SUCCESS_REDIRECT_URL`/`FAILURE_REDIRECT_URL` required when `PAYMENT_PROVIDER=xendit` (currently `?? ""`). Env schema superRefine.
- [ ] **P3 docs:** RUNBOOK real-provider swap section; API-REFERENCE payment webhook/refund notes; CONTEXT.

---

## PR P4 — Fail-loud guards + ops docs for real-provider swap

- [ ] **4.1 (Resend/X2)** `packages/env/src/server.ts`: `superRefine` → `RESEND_API_KEY` required when `NODE_ENV === "production"` (and `EMAIL_FROM` must be a non-default verified domain, or at minimum document verification). Add a boot-time note; RUNBOOK documents Resend domain verification for `cogitoacademy.id` (+staging subdomain).
- [ ] **4.2 (Google Meet/X3)** `services.ts`/env: `GOOGLE_MEET_ENABLED=true` requires a complete credential set (OAuth triple OR SA email+key+impersonatedUser+calendarId); require `GOOGLE_IMPERSONATED_USER` when SA mode selected (or explicitly document own-calendar mode); add a boot-time calendar probe (`calendarList.get` or create a throwaway event) that logs loud on failure. Fix `infra/.env.prod.example` (add `GOOGLE_IMPERSONATED_USER`, `GOOGLE_MEET_*`). RUNBOOK: OAuth refresh-token acquisition steps + scopes.
- [ ] **4.3 (R2/X4)** env superRefine: when `NODE_ENV === "production"`, require all four `R2_*` vars AND `R2_PUBLIC_URL` (fail loud instead of local-disk fallback / unreachable objects); add R2 vars to `infra/.env.prod.example`. RUNBOOK: R2 bucket + API-token setup section.
- [ ] **4.4 (G2)** Email verification via better-auth `emailVerification` plugin + Resend wiring + UI — **only after 4.1** (it requires a working Resend production sender). Decide scope with the user; may be a separate PR.
- [ ] **P4 docs:** RUNBOOK; `infra/.env.staging.example` (add `STUB_WEBHOOK_ALLOWED=true`, R2/GOOGLE_MEET placeholders); CONTEXT env notes.

---

## Shared Guidance

- **Docs follow code (AGENTS.md rule 11):** update `docs/CONTEXT.md`, `docs/RUNBOOK.md`, and plan statuses in every PR.
- Keep the suite green after each step (check-types, lint, oxfmt, targeted tests, full suite at PR end).
- Coverage gates: api ≥ 90% lines, overall ≥ 80%.
- Conventional commits; one commit per task or small coherent group.
- If a fix requires a migration, generate with `bun run db:generate` and review the SQL; never drop columns with live data without a backfill plan.
- Xendit changes are **not** verifiable against a live account without sandbox credentials — ask the user to provision Xendit test keys before PR P3 QA, and gate production enablement on a sandbox E2E.

### Version Notes

- v1.0 (2026-08-17): Created from the wave-4 full audit (docs + `.superpowers/sdd` reconciliation, PRD-alignment + money-correctness bugs C1–C3/H1–H6/M1–M9/L1–L5, third-party readiness X1–X5). All findings verified in code at `6c80391` and against current vendor API docs (Xendit OpenAPI 2024-11-11 fetched 2026-08-17).
