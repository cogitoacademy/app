# CI Sanity & False-Positive Elimination — Plan

| Field      | Value                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                     |
| ---------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| Status     | **F1–F7 implemented + merged 2026-08-31** (#130, #132–#137); F1 blind-spot fix (Dependabot) merged 2026-09-01; F6 verdict recorded 2026-09-01; PR #152 refreshes the exact oxlint 1.80 baseline, fixes three new React findings, and makes the lint auto-fix push hook-safe; F14 CI test/coverage performance is implemented on `f/ci-test-coverage-performance`; F8 (e2e in CI) + F10 (branch protection) remain; F9 (ACTIONS_BOT_PAT) operator console; F13 (lint-warning triage) documented 2026-08-31 |
| Created    | 2026-08-31                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                |
| Depends on | #126 merged (ops.sh + APPLY-RUNBOOK + this plan's trigger); main `85841b0`                                                                                                                                                                                                                                                                                                                                                                                                                                |
| Scope      | CI workflow fixes (fail-loud infra-plan, web-deploy verification, auto-rollback, dedupe, staging decision, test/coverage performance) + DLQ age-aware health                                                                                                                                                                                                                                                                                                                                              |

## Why

During the 2026-08-31 ops wave, several CI failure modes were discovered that
produce **false confidence**: jobs render green while the thing they were
meant to verify never ran. Evidence was gathered live (run logs, secret
lists, workflow sources) — not assumed.

## Verified findings (evidence-based)

| ID  | Finding (verified)                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                     | Evidence                                                                                                                                                                             |
| --- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ |
| F1  | `infra-plan.yml` references `R2_STATE_ACCESS_KEY_ID`/`R2_STATE_SECRET_ACCESS_KEY` GitHub secrets that **do not exist**. The credentials exist as `R2_ACCESS_KEY_ID`/`R2_SECRET_ACCESS_KEY` (per user, 2026-08-31). Result: plan step **silently skips** ("tokens not configured") and the job shows green.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                             | `gh secret list` (no `R2_STATE_*` entries); run 33358884750 step "Terraform plan (read-only): skipped"; infra-plan.yml lines 75–78                                                   |
| F2  | First push of #126 proved the skip mechanism hides real breakage: with partially-set R2 state creds, `terraform init` hit the state bucket and failed `403 Forbidden` on `terraform.tfstate` — a real error, later replaced by an unconditional skip.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                  | failed run 33358781690 log                                                                                                                                                           |
| F3  | Web deploy is **unverified**: `cd-prod.yml` POSTs the web resource webhook and stops. No health poll, no version check. A broken web image deploys silently.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                           | cd-prod.yml "Trigger Coolify web deploy"; scripts/migrate-and-deploy.sh verifies only the API `/health` version                                                                      |
| F4  | Rollback is a **printed hint**, not an action. On health-poll timeout the script tells a human to repoint Coolify at `v<prev-sha>`. No `if: failure()` auto-rollback step.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                             | migrate-and-deploy.sh `die "…ROLLBACK: point the Coolify server resource…"`                                                                                                          |
| F5  | CI **double-runs every merge**: `ci.yml` triggers on both `pull_request` and `push: [main, staging]` — every squash-merge re-runs the full 4-job suite on the merge commit.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                            | ci.yml `on:` block                                                                                                                                                                   |
| F6  | Dependabot bun-1.4 PRs (#98–#101) failed in **2–4 seconds** including jobs that never touch bun (`label`, `auto-merge`) → stale-base/infrastructure shape, not "bun 1.4 breaks the build" (unproven either way). Logs expired (404); bases predate the 08-27 CI churn. `recreate` issued 2026-08-31; verdict pending fresh runs. **VERDICT (2026-09-01, all old PRs recreated/superseded):** Dependabot reds come from three distinct mechanisms — (1) **infra-plan cred gate** (F1 blind spot, fixed 2026-09-01): `dependabot/*` same-repo PRs get NO repository secrets (same untrusted class as forks) but the gate only exempted `head.repo.fork`, so PRs #142/#144 failed with all four secrets "unset"; (2) **stale-base lint** (e.g. #71 nginx bump, 6s lint fail on a pre-2026-08-27-format base) — remedy `@dependabot recreate`; (3) **Cloudflare Workers external check flake** (0-second fail on a deps-only PR #145 — same check passes in 0s on other PRs) — remedy `@dependabot recreate`, then re-review. #142 (upload-artifact v6→v7), #144 (setup-terraform v3.1.2→v4.0.1) and #145 (17-dep group: vite 8.2.1→8.2.2, plugin-react 6.0.5→6.1.1, lefthook/turbo/aws-sdk/googleapis minors — all minor/patch, no majors) are safe to merge once their checks are green. | `gh pr checks 98..101` (2s failures); `gh pr checks 142/144` (infra-plan fail, log: "unset on a non-fork PR"); `gh pr checks 145` (Workers fail 0s); #145 diff vs main (minors only) |
| F7  | `cd-staging.yml` exists with staging webhook secrets pointing at the **same public host as prod** (`cl.cogitoacademy.id`) and no staging infrastructure exists. If anyone sets those secrets, staging deploys would hit the prod Coolify instance.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                     | cd-staging.yml lines 46–50; no staging host in Terraform/DNS                                                                                                                         |
| F8  | No e2e in CI — `packages/e2e` (10 specs) ran only in the 2026-08-26 local session.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                     | .github/workflows/ci.yml (no e2e job)                                                                                                                                                |
| F9  | S8 approval gate: the lint auto-commit bot (git-auto-commit-action, now v7 after #73) triggers `action_required` on every PR it touches while `ACTIONS_BOT_PAT` is unset — re-verified live on #126 (run 33358781690 → gate).                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                          | #126 run history 04:56:52Z; `gh secret list` (no `ACTIONS_BOT_PAT`)                                                                                                                  |
| F10 | No branch protection on `main` (API 404) — nothing requires the latest run of the merge commit to be green; stale-base merges are structurally possible. **Operator console action, not a PR.**                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                        | `gh api repos/.../branches/main/protection` → 404                                                                                                                                    |
| F13 | 123 pinned-toolchain lint warnings are triaged 2026-08-31: `no-await-in-loop` (53) **INTENTIONAL** — sequential money/DB writes in booking/wallet paths, parallelizing would risk money correctness; `consistent-function-scoping` (35) documented style; `no-underscore-dangle` (13) convention. Config-level removal of `no-await-in-loop` from CI output is not used because `.oxlintrc.json` is shared with the local run, and a CI-only suppression would hide new violations. CI now pins oxlint 1.80.0/oxfmt 0.65.0; the remaining known React compiler errors are recorded at their current locations in `.github/lint/baseline.txt`. PR #152 fixed three newly introduced findings (`profile-image-picker`, `onboarding-form`, and `tutor-experiences`) and sets `LEFTHOOK=0` on the auto-fix push so the dedicated baseline/format gates run without a recursive local hook. The local pre-push hook now runs the same baseline gate instead of raw `bun run lint`, preventing documented legacy errors from blocking every push while preserving failure on new errors. `bunx oxlint@1.80.0 --format=github`; `.oxlintrc.json` (shared local+CI config, no per-run override)                                                                                                |

## Fixes (this plan)

1. **F1/F2 — fail-loud infra-plan with correct secret names** (`.github/workflows/infra-plan.yml`):
   - Read R2 state creds from `R2_ACCESS_KEY_ID` / `R2_SECRET_ACCESS_KEY` (the names that actually exist) + keep `R2_STATE_ENDPOINT`.
   - Replace skip-with-notice by **fail with a precise message** when the PR is not from a fork and creds are missing. Fork PRs (no secret access) keep the skip.
   - The `403` class (wrong-permission token) must surface as a job failure, never a skip.
2. **F3 — web deploy verification**: after the web webhook POST, poll `https://app.cogitoacademy.id` (HTTP 200, bounded attempts, same script style as the API poll). Web nginx image gains a tiny `GIT_SHA`-bearing health page or the poll accepts 200 + content marker — decide during implementation, document choice.
3. **F4 — auto-rollback action**: add an `if: failure()` CD step that calls the Coolify API (via `COOLIFY_API_TOKEN`) to repoint the failing resource at `v<prev-sha>`; keep the printed hint as fallback copy. Never auto-restore DB snapshots (locked decision).
4. **F5 — CI dedupe**: drop the `push: [main, staging]` trigger from `ci.yml` (PR runs already gate every merge) or use `paths-filter`/concurrency dedup — pick and document.
5. **F7 — staging decision**: default = **delete `cd-staging.yml`** (locked: "prod first, no staging"). If user wants staging later, it returns with its own host.
6. **DLQ age-aware health** (folded from MONITORING-ALERTING): **DONE 2026-08-31 on `f/dlq-age-health`** — `checkDlqHealth` counts only entries with push-time `failedAt` newer than the freshness window (24h default, `DLQ_FRESH_WINDOW_HOURS` override); the stale pre-timestamp ledger reports `dlqDepth: 0`. Files: `packages/api/src/lib/db-health.ts` + `scheduler.service.ts` (failedAt stamp) + tests (100% line gate applies).
7. **F8 — e2e in CI**: out of scope here (needs runner + seeded DB wiring); tracked under MONITORING-ALERTING follow-ups.

## Implemented fixes (2026-08-31, ops/ci-deploy-verify)

Implementation deltas vs. the original fix sketches (all documented):

- **F1** — implemented exactly as planned, plus: the fail-vs-skip branch checks `github.event.pull_request.head.repo.fork == false` (non-fork ⇒ fail; fork ⇒ skip with a printed reason), the failure prints a `::error` annotation + per-secret remediation lines, and the skip path is now exclusively fork-scoped. Secret names switched to the existing `R2_ACCESS_KEY_ID` / `R2_SECRET_ACCESS_KEY` (+ `R2_STATE_ENDPOINT`); no `R2_STATE_ACCESS_KEY_ID` / `R2_STATE_SECRET_ACCESS_KEY` references remain anywhere in the repo.
- **F1 blind-spot fix (2026-09-01, ops/monitoring-discord wave)** — the fork-only exemption broke Dependabot: same-repo `dependabot/*` PRs also receive **no repository secrets** (GitHub's untrusted pull_request event classes deny secrets to both forks and Dependabot branches — verified live: PRs #142/#144 failed with all four plan credentials "unset"), so the gate hard-failed every dependency update that touched `infra/**`. The gate now reads `github.event.pull_request.head.ref` and skips (with a printed reason) for `IS_FORK == true` OR `HEAD_BRANCH == dependabot/*`; human branches keep the fail-loud path. Header comment + skip-step notice updated to match.
- **F3** — implemented via `scripts/migrate-and-deploy.sh --poll-web`: cd-prod.yml POSTs the web webhook then invokes the script in verification-only mode polling `HEALTH_URL_WEB` (default `https://app.cogitoacademy.id`) for plain HTTP 200 (bounded 20×15s, same shape as the API poll). **Choice documented: HTTP 200 only, no version marker** — the web image is static nginx with no `/health` endpoint and no version-bearing page (adding a `GIT_SHA` health page was rejected as image churn for zero behavioral gain; 200 is the strongest available signal and still catches crash loops, broken nginx config, and 5xx). The step keeps the unset-webhook guard. Deploy job timeout raised 15→25 min to fit both bounded polls.
- **F4** — **reverted 2026-09-02 to last known successful CD state `bb1ccb9a`.** The attempted application PATCH failed because the token lacks `write`; the follow-up VPS-side immutable image pull exhausted disk and crashed the self-hosted runner. `.github/workflows/cd-prod.yml` and `scripts/migrate-and-deploy.sh` are restored exactly from `bb1ccb9a`. Production again uses its configured webhook and keeps image pulls under Coolify ownership. Further rollback automation requires a separate reviewed design; DB snapshots remain never auto-restored.
- **F5** — implemented as the trigger-drop option: `ci.yml` now triggers only on `pull_request` (+ `workflow_dispatch`). All required check contexts survive (`lint`, `Type Check`, `Build`, `Test + Coverage`, `Terraform validate + plan`, `Ansible syntax-check`, `Plan-only audit docs present`, labeler, Semantic PR, auto-merge) because they hang off `pull_request` in their own workflows. cd-prod's `push: [main]` trigger is untouched (staging has no active pipelines).
- **F7** — `.github/workflows/cd-staging.yml` deleted (locked decision, see Status log).

## F14 — CI test and coverage performance (2026-09-02)

Implemented on `f/ci-test-coverage-performance`:

- Restore the shared Bun install cache before `bun install` in every CI job;
  keep separate Turbo caches for typecheck and build, with optional remote
  cache credentials.
- Use full history and Turbo `--affected` for pull-request typecheck/build;
  manual dispatches continue to run the full graph.
- Remove the web production build from `check-types`; the Build job remains the
  owner of the Vite production build.
- Start Test + Coverage independently from lint/typecheck, run one lcov
  coverage suite, keep the server suite in its separate `mock.module` process,
  and explicitly fail if the coverage test command fails.
- Remove the duplicate uninstrumented API pass while retaining the env/auth/db
  coverage scope and 100% lcov gate.

The local workflow-equivalent benchmark measured the old duplicate API pass at
116.24 seconds. With that pass removed, the test/coverage phase is expected to
save that duration; the exact GitHub Actions wall-clock time remains runner and
cache dependent.

## Operator console actions (user, not code)

- [ ] Branch protection on `main`: require status checks (`lint`, `Type Check`, `Build`, `Test + Coverage`, `Terraform validate + plan`, `Semantic PR`, `Labeler`) + **require branches up to date** (kills stale-base merges).
- [ ] Optional: `ACTIONS_BOT_PAT` (fine-grained, contents:write) to kill the S8 gate class; else accept per-PR approvals.
- [ ] After the cl-domain rename merges: recreate `COOLIFY_PROD_SERVER_WEBHOOK` + `COOLIFY_PROD_WEBHOOK` with `https://cl.cogitoacademy.id/api/v1/deploy?uuid=...`.

## Exit gates

- infra-plan job fails loudly (with exact remediation message) when creds are absent on non-fork PRs; runs a real `terraform plan` when they exist.
- A merged commit where the web image is broken makes CD red (and auto-rolls back), not silent.
- No duplicated CI suite on merge commits; dependabot PRs either merged green or closed with a documented reason.
- Pull-request typecheck/build use the affected graph, while manual dispatches use the full graph; Test + Coverage has no duplicate API execution and still fails on test or coverage-gate errors.
- All docs synced (README plans table, CONTEXT, RUNBOOK) in the wave PR.

## Status log

- 2026-08-31: plan created after live verification; #126/#73 merged; dependabot `recreate` posted on #98/#99/#100/#101/#71.
- 2026-08-31 (worker `ops/ci-deploy-verify`): **F1, F3, F4, F5, F7 implemented.**
  - F1: `infra-plan.yml` now reads `CLOUDFLARE_API_TOKEN` / `R2_ACCESS_KEY_ID` / `R2_SECRET_ACCESS_KEY` / `R2_STATE_ENDPOINT` — the `R2_STATE_ACCESS_KEY_ID` / `R2_STATE_SECRET_ACCESS_KEY` GitHub secrets never existed (user-confirmed 2026-08-31; the operator's existing R2 token is the correct credential). Credential check now **fails the job (exit 1, `::error` + remediation lines)** when any of the four is unset on a **non-fork** PR (`github.event.pull_request.head.repo.fork == false`); fork PRs keep the skip path (no secret access on fork events). A 403 on the state bucket now surfaces as a red job — never a silent skip. Header comment block rewritten.
  - F3: `migrate-and-deploy.sh` gained `--poll-web` (env-driven `HEALTH_URL_WEB`, default `https://app.cogitoacademy.id`; bounded 20×15s poll for **HTTP 200 only** — the web image is static nginx with no version marker; rationale in the script header). `cd-prod.yml` passes `HEALTH_URL_WEB` and runs `--poll-web` right after the web webhook POST; a broken web image now turns CD red. Deploy job timeout 15→25 min.
  - F4 historical note: the native deploy-only experiment was reverted on 2026-09-02. Current code is restored from `bb1ccb9a`: configured webhook deploy plus the original best-effort application PATCH rollback. The current token may reject rollback PATCH with 403; manual rollback remains the fallback and **DB snapshots are never auto-restored (locked)**.
  - F5: `ci.yml` triggers reduced to `pull_request` (+ `workflow_dispatch`); all required check contexts survive (verified below). No other workflow's triggers touched.
  - F7: **`cd-staging.yml` DELETED (locked decision: prod-first, no staging exists).** Rationale: its staging webhook secrets (`COOLIFY_STAGING_SERVER_WEBHOOK` / `COOLIFY_STAGING_WEBHOOK`) are unset; if ever set, the example URLs pointed at the **same prod Coolify host** (`cl.cogitoacademy.id`) — a "staging deploy" would redeploy over the production Coolify instance. There is no staging host in Terraform/DNS and no staging Coolify project; pushing to `staging` now runs no CD at all. If staging returns, it comes back with its own host + its own Coolify instance. Decision recorded in RUNBOOK (Deploy Secrets + GHCR/CD sections) and DEPLOYMENT.md.
  - Verification: all touched YAML parsed (python3 yaml.safe_load); `bash -n` clean on the script; dry-run modes + mock-Coolify rollback tests + live HTTP-200 poll tests pass; `rg` finds zero `R2_STATE_ACCESS_KEY_ID` / `R2_STATE_SECRET_ACCESS_KEY` references repo-wide.
  - Still open in this plan: F2 (superseded by the F1 fail-loud behavior — retained for evidence), F8 (e2e in CI, deferred), F9 (`ACTIONS_BOT_PAT` operator decision), F10 (branch protection, operator console). DLQ age-aware health (fixes item 6) remains dispatched separately.
- 2026-09-01 (lead, `ops/monitoring-discord` wave): **F1 blind-spot fix + F6 verdict recorded.** `infra-plan.yml` cred gate now exempts `dependabot/*` head branches (skip + printed reason; humans still fail loud) — Dependabot PRs receive no repository secrets, same as forks. F6 verdict: three red mechanisms (infra-plan gate — fixed; stale-base lint → `@dependabot recreate`; Cloudflare Workers external-check flake → `@dependabot recreate`). #142/#144/#145 safe to merge once green (all minor/patch).
- 2026-08-31 (worker `ops/ci-deprecation`): **F13 added and triaged** — the 123 pinned-toolchain lint warnings (no-await-in-loop 53 / consistent-function-scoping 35 / no-underscore-dangle 13 / remainder) are documented-intentional (see the F13 row). Config-level removal of `no-await-in-loop` from CI output was evaluated and **not implemented**: `.oxlintrc.json` is shared with the local run, so a per-rule severity downgrade would silence the warnings locally too (violates the "NOT from the local run" constraint), and a CI-only `-A` flag would hide new real violations. Left as documented warnings; the CI lint job still exits 0 (warnings are not errors on the pinned toolchain). Also in this wave: `actions/checkout` v4→v6 (kills the recurring "Node 20 is being deprecated" warning — v6 runs Node 24), floating action tags pinned to concrete patch tags on the same major (setup-bun v2.2.0, merge-dependabot v3.15.0, docker/login-action v4.6.0, setup-terraform v3.1.2), and the Ansible inventory group `[cogito-vps]` renamed to `[cogito_vps]` (kills the "Invalid characters were found in group names" warning; inventory + playbooks in one atomic change).
- 2026-09-02 (PR #152 CI follow-up): **lint and CodeQL hardening verified.** The baseline was refreshed to the exact oxlint 1.80.0 locations after the tutor-profile branch merge; three branch-introduced React compiler errors were removed from source. The CI lint auto-fix push now sets `LEFTHOOK=0`, leaving `check-baseline.sh` and `oxfmt --check` as the authoritative gates. The admin's manually pasted photo URL is shown only as readiness text rather than a direct image source; local `blob:` previews in the profile picker remain intentional file previews, and the corresponding CodeQL findings were dismissed as false positives. PR #152's lint, typecheck, build, test/coverage, CodeQL, Workers, and PR automation checks are green.
- 2026-09-02 (local push follow-up): **pre-push lint gate aligned with CI.**
  Lefthook and CI now run the cross-platform
  `.github/lint/check-baseline.ts` instead of raw `bun run lint`; documented
  legacy React findings remain visible but no longer
  reject every local push, and any non-baselined lint error still fails. The
  gate also removes its temporary GitHub-format output on exit.
